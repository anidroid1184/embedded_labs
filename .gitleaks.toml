title = "embedded_labs"

[extend]
useDefault = true

[allowlist]
description = "Local-dev placeholders only (never real secrets)"
paths = [
  '''^\.env\.example$''',
]
regexes = [
  '''changeme''',
]
